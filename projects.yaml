# Branch Manager - Project Registry

projects:
  # Placeholder - add your projects here
  # Format: name: repo: "...", env: ".env.name", defaultBranch: "main"
  test:
    repo: https://github.com/2urly/test.git
    env: .env.test
    defaultBranch: main